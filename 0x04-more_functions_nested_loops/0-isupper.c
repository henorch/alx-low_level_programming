#include "main.h"
#include <stdio.h>
/**
 *_isupper- check if the input is upper
 *@c: this hold a variable
 *Return: This return the value of 1 or 0
 */

int _isupper(int c)
{
if (isupper(c))
{
return (0);
}
else
{
return (1);
}
