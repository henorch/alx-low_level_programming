#include "main.h"

/**
 *mul- This multiply two numbers
 *@c: It holds the first variable
 *@d: It holds the second variable
 *Return: It return the mul val
 */

int mul(int c, int d)
{
return (c * d);
}
