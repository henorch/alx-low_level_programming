#include "main.h"
/**
 * _strlen- length of string
 * @s: the pointer
 * Return: int
 */

int _strlen(char *s)
{
char str[100];
str[100] = *s;	
char cnt;
cnt = sizeof str;
_putchar(cnt - 1);
return (0);
}
